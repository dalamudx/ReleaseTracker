name: Build Docker Image

on:
  workflow_dispatch:
    inputs:
      branch:
        description: '构建的分支名称'
        required: false
        default: ''
      tag_latest:
        description: '是否标记为 latest（同时会提取并打上版本标签）'
        required: false
        type: boolean
        default: false
      tag_dev:
        description: '是否标记为 dev'
        required: false
        type: boolean
        default: true
      custom_tag:
        description: '自定义标签（可选）'
        required: false
        default: ''

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: 确定构建分支
        id: branch
        run: |
          if [ -n "${{ github.event.inputs.branch }}" ]; then
            echo "branch=${{ github.event.inputs.branch }}" >> $GITHUB_OUTPUT
          else
            echo "branch=${{ github.ref_name }}" >> $GITHUB_OUTPUT
          fi

      - name: Checkout 代码
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.branch.outputs.branch }}

      - name: 提取版本号
        id: version
        run: |
          BRANCH="${{ steps.branch.outputs.branch }}"
          # 尝试从分支名提取版本号（支持 2.1.1 或 release/2.1.1 格式）
          VERSION=$(echo "$BRANCH" | grep -oP '\d+\.\d+\.\d+' || echo "")
          
          if [ -n "$VERSION" ]; then
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            # 提取主版本和次版本
            MAJOR=$(echo "$VERSION" | cut -d. -f1)
            MINOR=$(echo "$VERSION" | cut -d. -f1-2)
            echo "major=$MAJOR" >> $GITHUB_OUTPUT
            echo "minor=$MINOR" >> $GITHUB_OUTPUT
            echo "✅ 提取到版本号: $VERSION (主版本: $MAJOR, 次版本: $MINOR)"
          else
            echo "version=" >> $GITHUB_OUTPUT
            echo "major=" >> $GITHUB_OUTPUT
            echo "minor=" >> $GITHUB_OUTPUT
            echo "⚠️  未能从分支名提取版本号"
          fi

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录到 GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 生成 Docker 标签
        id: meta
        run: |
          TAGS=""
          
          # tag_latest: 添加 latest 和版本标签
          if [ "${{ github.event.inputs.tag_latest }}" == "true" ]; then
            TAGS="$TAGS,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
            
            if [ -n "${{ steps.version.outputs.version }}" ]; then
              TAGS="$TAGS,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.version }}"
              TAGS="$TAGS,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.minor }}"
              TAGS="$TAGS,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.major }}"
            fi
          fi
          
          # tag_dev: 添加 dev 标签
          if [ "${{ github.event.inputs.tag_dev }}" == "true" ]; then
            TAGS="$TAGS,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:dev"
          fi
          
          # custom_tag: 添加自定义标签
          if [ -n "${{ github.event.inputs.custom_tag }}" ]; then
            TAGS="$TAGS,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.event.inputs.custom_tag }}"
          fi
          
          # 移除开头的逗号
          TAGS=$(echo "$TAGS" | sed 's/^,//')
          
          echo "tags=$TAGS" >> $GITHUB_OUTPUT
          echo "🏷️  生成的标签: $TAGS"

      - name: 构建并推送 Docker 镜像
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 输出构建信息
        run: |
          echo "### 🎉 构建完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**分支:** ${{ steps.branch.outputs.branch }}" >> $GITHUB_STEP_SUMMARY
          echo "**版本:** ${{ steps.version.outputs.version || '未提取' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**镜像标签:**" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" | tr ',' '\n' >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
